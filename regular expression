<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
        //quantifiers specify how many times a character or group should appear
        let text="aaaba";
        let pattern=/\a?/g;
        let result=text.match(pattern);
        console.log(result);
        //modifiers(flags)
        let text1="Hai Hello How are you?\nHai Im Fine.Hai";
        let pattern1=/^hai/igm;
        let result1=text1.match(pattern1);
        console.log(result1);
        /*Meta Characters:
        Meta characters have special meaning in regex.
        Common Meta Characters:
        .  ->any character except newline
        \d ->digit(0-9)
        \w ->word character(letters,digits,-)
        \s ->whitespace
        ^  ->start of string/line
        $ */
        let number ="2468abcsdef"
        let pattern2=/\d+\w+/g;
        let result2=number.match(pattern2);
        console.log(result2);
        /*methods:
        test() ->quick check: does te string contain the pattern?
        exec() ->gives detailed info about the first match(and captured groups)*/
    </script>
</body>
</html>
